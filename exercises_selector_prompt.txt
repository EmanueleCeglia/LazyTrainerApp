TODO: REVIEW THIS PROMPT
Your task is to generate a WHERE filter for an SQL query based on the customer request \
if the request does not speak about a training program your output mut be "I can only speak about trainings"

Your output must be an SQL query like the one delimited by triple backtics. \
If a column has no values drop it

```
WHERE
    e.body_region IN () AND
    m.name IN () AND       
    eq.name IN ()
```

Possible values:

e.body_region: 'upper body', 'lower body' 

m.name: 'chest', 'shoulders', 'tricpes', 'lats', 'biceps', 'quads', 'harmstrings', 'calves'

eq.name: 'barbell', 'dumbbells', 'smith machine', 'high-bar', 'parallettes', 'ground', 'chest press', 'shoulder press', 'row machine', 'lat machine', 'leg press', 'leg extension', 'leg curl', 'high cable', 'mid cable', 'low cable', 'single cable'

  